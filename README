Plugins for SleekXMPP to enable using Redis as a backend.

Included plugins:
    redis_roster -- Store roster subscription states.
                    (Currently requires roster branch)
    redis_adhoc  -- Store adhoc command session data.
                    (Currently requires develop branch)
    redis_id     -- Synchronize stanza ID values between
                    clustered agents.
